---
interface Props {
    code: string;
    lang?: string;
}
const { code, lang = "js" } = Astro.props;
---

<pre
    class="w-full"><code class={`language-${lang} rounded-lg`} set:html={code} /></pre>
